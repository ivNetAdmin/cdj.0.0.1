$(document).ready(function () {
    $(window).on('load scroll', function () {
        var scrolled = $(this).scrollTop();
        // $('#title').css({
        //     'transform': 'translate3d(0, ' + -(scrolled * 0.2) + 'px, 0)', // parallax (20% scroll rate)
        //     'opacity': 1 - scrolled / 400 // fade out at 400px from top
        // });
        //$('.btn-holder').addClass('hide');
        $('#hero-vid').css('transform', 'translate3d(0, ' + -(scrolled * 0.25) + 'px, 0)'); // parallax (25% scroll rate)
        
        setTimeout(function(){
        $('.splash').removeClass('hide');
        $('.btn-holder').removeClass('hide');
        
        },2500);

    });

    $('#hero-vid').on('loadeddata',function(e)
    {
       //alert("hello mum");
    });

    var i=0;
    var running=false;
    function parallax_loop()
    {
        $('.screen-size').html(" ("+navigator.userAgent+")");
       // alert("w:" + $(window).width() + ", h:" + $(window).height());
        var parallaxBG = $('.video-fallback-carousel-section');
        setTimeout(function(){
        if(running)
        {
            switch(i)
            {
                case 1:
                parallaxBG.removeClass('parallax1');
                parallaxBG.addClass('parallax2');
                break;                
                case 2:
                parallaxBG.removeClass('parallax2');
                parallaxBG.addClass('parallax3');
                break;                
                case 3:
                parallaxBG.removeClass('parallax3');
                parallaxBG.addClass('parallax1');
                break;
            }
        }
       
        running=true;
        if(i==3)
        {
            i=0;
        }
        parallax_loop();
        i++; 
    }, 3500);
        
    }
    parallax_loop();

    // new Tether({
    //     element: '.splash',
    //     target: '.overlay',
    //     attachment: 'top left',
    //     targetAttachment: 'top right',
    //     constraints:[{
    //         to: 'window',
    //         pin: true}]
    // });

    // video controls
    // $('#state').on('click', function () {
    //     var video = $('#hero-vid').get(0);
    //     var icons = $('#state > span');
    //     $('#overlay').toggleClass('fade');
    //     // if (video.paused) {
    //     //     video.play();
    //     //     icons.removeClass('fa-play').addClass('fa-pause');
    //     // } else {
    //     //     video.pause();
    //     //     icons.removeClass('fa-pause').addClass('fa-play');
    //     // }
    // });
});